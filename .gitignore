# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files — never commit real secrets. .env.example is OK.
.env
.env.*
!.env.example

# Throwaway scratch — verification harnesses, one-off launchers, etc.
public/teller-connect.html

# One-off DB probes, migrations and data backups. Never tracked by convention —
# they contain real transaction data and are specific to one machine's .env.local.
# (Accidentally committed in 1fbbcaa and untracked immediately after.)
scripts/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
