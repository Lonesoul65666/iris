---
name: project-iris-handoff-2026-07-01
description: Iris handoff 2026-07-01 — Money Map v2 (zero-based actual + confirmable investing) + stash goals/ETA + month-rollover fix + Budget Pulse persists per-month + 5-agent budget-engine AUDIT (docs/budget-engine-map.md) + work-bucket data cleanup + Work In/Out ledger. Locks major model DECISIONS. READ FIRST.
metadata: 
  node_type: memory
  type: project
  originSessionId: efbb01bc-7c92-4b56-91a0-0b226ddccacf
---

# Iris handoff — 2026-07-01 (guts audit + model decisions)

**MOST RECENT. Read first** (supersedes 2026-06-30 as read-first; that one's fix-queue/design detail still valid). Branch `overnight-polish-2026-06-11`, repo `C:\Claude\projects\signal\signal-app`, detached vite on :5173. **~54 commits over master, unmerged.** `npx tsc -b` clean · 126/126 tests. Long session, Scott reacting in his real Chrome (HMR) — validated everything live via the Claude-in-Chrome extension (I drive it). ⏰ Deadline still ~Aug 2026 (Scott leaves Abnormal, loses Claude); portability still #1 deadline item but this session was all budget-guts (the thing that dies without me).

## Shipped this session (7 commits: 51ecd0d → bd596f3)
1. **Money Map v2** — reframed to the zero-based ACTUAL "how's the month going" off the $15,800 base (everyday SPENT + investing + reserves → leftover=the win). Killed the bottom-up "$14,584" headline. Investing lane shows planned/dashed + a **Confirm deposit** button (`deployConfirmations` collection, per-month) — but it's **COSMETIC** (only slice styling; "confirming doesn't change the math"). Scott: cosmetic is FINE.
2. **Stash goals + ETA** — `computeStashForecast` (`stashMath.ts`) on the DERIVED balance; StashesCard now shows % of goal, $ to go, and a forward ETA ("Funded ~May 2036 · N mo" / "On track for X" / "Behind — $Y/mo more" / "Goal met"). No target → "+ Set a goal" nudge. (Scott's stashes have NO targets set yet — his data homework.)
3. **Month-rollover fix** — current calendar month is ALWAYS navigable (was invisible until it had a txn → stranded on last month on the 1st). Default "latest" = **most recent month WITH ACTIVITY** (not the blank new month) — so you open to real data and it auto-advances to the new month on its first txn (Scott's spec). `emptyMonthlySpending` for the blank current month. First-6-days **projection guard** (killed the day-1 "On Pace to Overspend −$15,200" nonsense).
4. **Budget Pulse persists per-month** — was hard-gated to in-progress; now "How the month is going" (live) vs "How the month WENT" (locked, no pace/trend/projection) for closed months. Continuity across ← → nav.
5. **5-AGENT BUDGET-ENGINE AUDIT → `docs/budget-engine-map.md`** — the wiring-level truth doc (didn't exist) + a **P0–P3 bug queue**. READ IT; it's the redesign roadmap.
6. **Work-bucket data cleanup** — pulled ~$7.6k of PERSONAL spend that was auto-flagged work (Baglioni resort $5,692, wife Lillah's flights+refunds $1,657, Uber Eats $142) → `travel_personal`/`food_dining`; flipped Fishmarket/Omni/Coronet to work-pending. Work bucket $30,477→$22,848; float now honest $998. **Backed up** (`scripts/work-reclassify-backup-2026-07-01.json` + `-refunds-` ). ⚠️ Only 6 items of ~275 triaged — full triage still pending.
7. **Work In/Out ledger** — rebuilt WorkReimbursementsCard: "Currently floating $998" hero + month-by-month Out/Back/Net (June-forward = the record Scott cares about). Work kept OUT of the $15,800 (a work trip isn't personal consumption) but now VISIBLE.

## ⭐ MODEL DECISIONS LOCKED THIS SESSION (not all built)
- **Base** = always Scott's guaranteed **base income**, robust across job changes. Fix the `perMonth = round(deposits/months)` over-multiply drift. **STOP freezing it** — must update as pay changes (currently frozen after first derive). [NOT BUILT]
- **grossMonthly** — currently the bad `base/0.72` (~$21,900); real ~$30k (deductions ~47% not 28%) → inflates savings rate, deflates housing ratio. **Fix from real data — NEEDS Scott's real monthly gross number.** [NOT BUILT]
- **Investing** — drive the bucket from the **REAL Fidelity feed** (`transactionType='investment'` txns → into the investing bucket, visible when they land, track up/down, audit-log). Today the $1,000 is force-written from Settings as "done" regardless. Confirm button staying cosmetic is FINE. [NOT BUILT — the feed-driven part]
- **Work model = COUNT IT, TRACK PAYBACK** (Scott chose this over "separate float"): visible money-out + visible payback, round-trip clear, but **kept OUT of the $15,800** so it doesn't falsely deflate the month. Work In/Out ledger = the delivery. Import classifier still auto-flags travel as work — fine for now since MOST of his travel IS work; the fix was making it visible, not reclassifying. Full backlog triage + smarter import default still pending.
- **$15,800 is THE frame** — Money Map is the ONLY surface that gets it right. Unify the others onto it: Budget Pulse header ($13,227 operating caps + a conflicting $15,800 sub-line), Dashboard donut (~$12,200), and Budget Health's gross-vs-net split. Also: Monthly Spend tile includes investing while Money Map "everyday" excludes it (differ ~$1k). [NOT BUILT — this is the "how the budget is displayed" redesign Scott wants]
- **Wife-confusion test** is a real design constraint (one number, no unexplained buckets, don't budget the variable/bonus).

## NEXT (priority order, per Scott)
1. **Investing → feed-driven** (decided, isolated) — resolver `investingActual(month)=confirmed?planned:(real investment txns else 0)`; stop force-writing monthlyActual; feed-validate. Call sites in `docs/budget-engine-map.md` §4.
2. **grossMonthly fix** — the moment Scott gives his real monthly gross.
3. **Base: un-freeze + fix perMonth over-multiply.**
4. **$15,800 denominator unification** (the display redesign) — make every surface consume Money Map's partition.
5. **Full work-bucket triage** + import default (stop auto-hiding; propose-don't-auto-decide) + **variable-earnings dashboard** (Scott will feed screenshots/expense-report exports; June-forward).
6. Then the deploy/gamification engine + Month-in-Review (per `docs/money-map-design.md`), and PORTABILITY (the deadline item).

## Update — later same session (commits through 56 over master)
- **grossMonthly DECISION RESOLVED + BUILT:** killed the fake `base/0.72` gross. Savings rate + housing ratio now compute off **NET take-home ($15,800)** (`savingsRate.ts` denominator → net; housing ratio → net AND month-specific `overviewBuckets`, fixing the HOUSING-AVG audit bug). Savings rate 9.9%→13.7% (of take-home), housing 20.3%. Scott: net is a hair under actual = conservative-safe, and it's the one-number frame. NO gross input needed ever.
- **Month over/under RECONCILED (Scott caught the mismatch):** Money Map said "over base −$1,504", Saved tile + Cash Flow said "+$496 under" — gap = the $2,000 reserve set-aside. Now ONE number everywhere: `monthSurplus = base − everyday − investing − reserveSetAside` drives the Saved/On-Pace tile (+ its projection + month-over-month) and the Cash Flow health sub-score, matching the Money Map. June = −$1,504 across the page (everyday ran hot $14,304 vs $12,727 budget). Winning-oriented: tile shows "May −$4,937 → ▲$3,433 better". **Decision: reserve set-asides ARE a job for the $15,800 (count them); over = everyday ran hot.**
- **$15,800-frame unification STATUS:** ✅ Money Map, Safe-to-Spend, tiles, savings rate, housing ratio, month over/under, Cash Flow score, **Dashboard cash-flow bar (reconciled — reserve segment + day-1 guard, matches Money Map partition).** ⏳ STILL open: **Budget Pulse header** ($15,304/$13,727 = spent-vs-operating-caps; arguably fine as detail, Scott said "come back to it"), **Dashboard donut** (~$12,200, cosmetic), **"spending exceeds income by $Xk/mo" insight** (`insightsEngine.ts` — AVERAGE-based + reserve-excluding = a different lens; needs a DECISION: reframe to the monthly $15,800 over/under, or soften to "you lean $X/mo on variable pay" for a variable-income household. Loudest remaining cross-page oddity).
- **NEXT — big fork (Scott raised 2026-07-01, then paused):** now that **Fable is back**, redo the UI to "look way better" — possibly before finishing the remaining plumbing. My rec: guts are coherent enough to redesign on honest numbers; do it surface-by-surface + wire investing-feed into the Money Map as we rebuild it (touch each once); **protect a portability window before August** (redesign is the redoable part; portability is what strands the app). NEEDED FROM SCOTT before starting: (1) sequence (redesign+plumb-together vs investing-feed-first vs portability-first) and (2) visual direction (refine-cyber vs clean/warm vs playful/game-like) — asked both, he dismissed/paused. Don't start the redesign until he picks a direction.
- **✅ INVESTING-FEED SHIPPED (2026-07-02, commit 58 over master):** investing counts ONLY when real — `investingActual = feed-seen(monthlyData.totalInvestments>0) ? real : confirmed(deployConfirmations) ? planned : 0`; overrides the investing bucket's monthlyActual in `overviewBuckets` so Money Map / Pulse / savings rate / month-over-under all agree. Planned-but-not-real is NOT deployed (stays in Free). MoneyMap: `investingStatus` 'feed'|'confirmed'|'planned' ('✓ from Fidelity' / '✓ confirmed'+undo / greyed 'planned'+Confirm deposit). Verified: June='✓ from Fidelity \$1,000' (real 6/29 FID txn), July=planned/Free \$13,800/Pulse UNTOUCHED, confirm flips Free→\$12,800, undo round-trips, persists. NOTE: only June's FID txn is imported (July's not drafted; deeper Teller history re-pull still rate-limited). Dashboard cash-flow bar still uses budgetSummary.investing (Settings \$1,000) — a smaller July lie, NOT yet switched to the resolver (follow-up).
- **DECISIONS (2026-07-02): deadline watermark = SEPT 1 (~2 months). Portability = LATER** (doing it first = repackage every change; localhost fine till near end). When we do it: **laptop/desktop first, mobile later; likely a hosted PWA** (auto-updates, installs on Claire's phone — she's 99% mobile) — so **build the redesign RESPONSIVE from the start** to make mobile cheap. **NEXT = THE UI REDESIGN, together (Fable).** Scott finds the current UI "terrible / not engaging"; wants my critique + a real overhaul. Still NEEDED: visual direction (asked refine-cyber vs clean/warm vs playful — he dismissed; re-engage with a critique + recommendation, don't force a menu).
- **Still NOT built:** base un-freeze + perMonth fix, full work-bucket triage + import default, variable-earnings dashboard, the "spending exceeds income" insight reframe, Dashboard donut + Dashboard investing resolver.

## ⭐ SESSION CLOSE (2026-07-02) — REDESIGN VERDICT + NEXT SESSION START HERE
- **NO full redesign. Fable is NOT available to Scott** (can't select it; running Opus/Sonnet). Big UI/UX overhaul is OFF. **Keep the current "cyber" UI**; only move a few things around on Dashboard + Budget.
- **Both Scott AND Claire REJECTED every mockup direction I tried** — the broken-out card app, the muscle-car instrument-cluster dash (chrome/gauges/carbon). Do NOT rebuild or revisit those. Lesson: **stop guessing "worlds."**
- **Redesign approach going forward = PRESCRIPTIVE from Scott's own screenshots.** He'll share reference images of designs he + Claire like; extract palette/type/layout/density/components and map OUR modules onto them. Paused until he provides them. (He also floated: use elements from those screenshots to restyle pieces incrementally rather than a big-bang redesign.)
- **⏭️ IMMEDIATE NEXT TASK (small, do first):** kill the cyber-grid + scanline "light animation" on (1) the **Dashboard "Net worth · all sources" hero** and (2) the **Budget "Net Take Home" tile** — it reads static even though values move in/out. Scott wants the net-worth window remade as a **"cool 3D bubble fade"** — **wait for his reference picture** before building it (don't guess). Files: `src/views/DashboardView.tsx` (net-worth hero, `cyber-grid`/`cyber-scanlines` classes), `src/components/Budget/BudgetView.tsx` Net Take Home tile (~line 1063, `cyber-grid cyber-corners cyber-scanlines`).
- **Throwaway:** `public/redesign-mockup.html` (rejected muscle-car/card mockups) — safe to delete.
- **Session token ~700k → starting fresh.** Dev server = detached vite on :5173 (drops sometimes; relaunch `Start-Process cmd /c "... vite.cmd --port 5173 --host > vite-dev.log"`). Chrome MCP server id churns (`claude-in-chrome` lowercase now) — reload tools via ToolSearch each session.

### What this session actually shipped (branch 46→58 over master, ~12 commits, 126/126 tests, tsc clean throughout)
Money Map v2 (zero-based actual + confirmable investing) → stash goals/ETA (`computeStashForecast`) → month-rollover fix + latest-with-activity default + first-week projection guard → Budget Pulse persists per-month → 5-agent budget-engine audit (`docs/budget-engine-map.md`) → work-bucket data cleanup (pulled ~$7.6k personal out; float now $998) → Work In/Out ledger → savings-rate + housing-ratio off NET take-home (killed the fake `base/0.72` gross) → month over/under reconciled across Money Map / tile / Cash Flow (reserve set-aside now counted, matches the whole-$15,800 view) → Dashboard cash-flow bar reconciled (reserve segment + day-1 guard) → investing-feed honesty (counts only when feed-seen OR confirmed; June real from FID, July planned). All browser-verified in Scott's Chrome.

## Standing facts / gotchas (unchanged + new)
- **Chrome nav flaky:** clicking the sidebar "Budget" at coord (68,120) after a reload often doesn't register (lands on Dashboard). Reliable path: `find` the "Budget" nav button → click by ref. Then verify page with `document.body.innerText.includes('Budget Health')`.
- **Bash cwd resets to `C:\Claude`** between calls — always `cd /c/Claude/projects/signal/signal-app &&` first (probe scripts, git). Foreground PowerShell keeps cwd.
- Expenses = JSONB in the `expenses` table (`data->>'field'`). Probe/patch scripts connect via `DATABASE_URL` in `.env.local`, ssl rejectUnauthorized:false. BACK UP before mutating (did).
- `travel_work` is a RESERVE lane AND name-filtered in many places (double-membership = latent regression hazard, see audit LANE-DUP).
- **Lane membership is NOT versioned** (targets are) — reconfiguring stashes rewrites past operating/reserve splits (audit LANE-VER).
- Verifying in Scott's Chrome: `location.reload()` via javascript_tool → wait → click Budget nav (sometimes needs 2 tries; click coord (68,120)). Month-nav: `←`/`→` arrows; JS to read the min-w nav button. REPL persists top-level `const` across calls (use unique names or top-level await).
- The 5 audit findings live in `docs/budget-engine-map.md` — P0: investing-shows-done (BUG-INV1) + work-classifier-hides-personal (BUG-W1, now partly cleaned).
