---
name: project-iris-handoff-2026-07-06-v2
description: "Iris handoff 2026-07-06 v2 — READ FIRST. Direction CORRECTED: NO investment pivot (Scott pulled it back); focus stays on the budget program via 5 BIG ROCKS (see roadmap). Shipped + committed (3f44223 on master): 'This Week's Focus' weekly action briefing + 'What's New' version-gated card, both in a top-of-dashboard briefing zone. Rock 1 first slice done. tsc clean, 250+ tests."
metadata: 
  node_type: memory
  type: project
  originSessionId: ea33235f-86b3-4f16-abe7-760a4215456b
---

# Iris handoff — 2026-07-06 v2 · READ FIRST

Supersedes [[project-iris-handoff-2026-07-06]] as read-first (its shipped context still valid). Repo `C:\Claude\projects\signal\signal-app`, on **master**. tsc clean · **250+ tests** · pre-commit runs tsc+vitest. Preview MCP server name is **`iris`** on :5173. **Same shared Postgres for preview :5173 AND Scott's Chrome** — dismissing cards in preview persists to his DB, so I avoid clicking "Got it"/"Dismiss" during verification.

## ⚠️ DIRECTION CORRECTED (Scott, 2026-07-06 evening) — the big thing
**The earlier "PIVOT READY → Investment" was premature. Scott pulled it back.** Investments AND equity stay dormant under `PHASE_1_LOCK`; lit up "slowly, later," NOT now. Focus stays 100% on the **budget program**. Full detail + the 5 big rocks now live in [[project-iris-gamification-roadmap]] ("DIRECTION LOCKED" section). Summary:
- **ONE PORTAL, COOPERATIVE-ONLY.** Drop competitive scoreboards + per-user/personalized state entirely (keep only the existing fun-money h2h). No per-user rewiring. This DELETES the per-user/h2h-comeback achievements from the queue and makes multi-machine cheap (PIN = a lock, not identity).
- **Cost-conscious mode:** Scott pays per usage — SCOPE each rock together before writing code; back-and-forth to avoid rebuild is preferred. Time is not a constraint, cost is.
- **THE 5 BIG ROCKS (order 1→5):** 1) finish budget program (AI feed-ins = regenerating action items, meet-in-middle tuning, fun-money flourishes, **cash-flow calendar + subscription radar** = budget features not investment); 2) gamification finish (coop-only, dedicated Achievements view+nav); 3) visual design (kill "same style box" + tooltips + tutorial); 4) update capability (GitHub/self-update); 5) onto other machines (git pull → point .env.local at shared DB → PIN → go; concurrent-write conflict DEFERRED — narrow 2-person risk).
- **Why budget before distribution:** Iris does NOT depend on this AI platform to RUN (plain Vite/React/Node/Postgres; in-app AI is BYOK). Losing the platform = losing the co-builder, not the product. Insurance = worth-opening-daily + self-runnable.

## ✅ Shipped this session (committed 3f44223 on master, browser-verified)
**Rock 1 first slice — the "AI feed-in" briefing.** Top-of-dashboard briefing zone, order: sync-health → What's New → achievement celebrations → **This Week's Focus** → hero.
1. **This Week's Focus** (`src/utils/weeklyBriefing.ts`) — pure CURATION over the `insights` already computed by `insightsEngine.generateInsights` (NO new detection). Picks the top budget moves by severity: **default 2, a 3rd ONLY for a genuine high-severity (critical/warning) need, hard cap 3**. **Frozen per Monday-anchored week** (`weekKeyOf`) → stable within the week, regenerates Monday, an item DROPS the moment it's resolved (falls out of the pool). Zero-AI templated voice. Each item deep-links to the exact Budget sub-tab (section encoded in the nudge id `briefing:<section>:<insightId>`). **Per-week Dismiss** (adds to `FrozenBriefing.dismissed`; re-evaluated next Monday — NO permanent kill, a budget alert should return if still true). On-demand LLM "weigh in" is served by the already-shipped Budget→Ask Iris (user-initiated; no auto token cost).
2. **What's New** (`src/updates.ts`) — the release-notes area. `UPDATES[]` list; newest entry's `version` gates a one-time `whatsNewNudge`. Version-gated vs persisted `last_seen_update_version`; "Got it" persists, silent otherwise. Doubles as Rock 4's payoff. **Bump the top entry's version when you ship to re-surface it.**
3. Wiring: `AppDataContext` exposes `weeklyBriefing`, `dismissBriefingItem`, `whatsNew`, `dismissWhatsNew` (off existing insights + settings; effects mirror the celebration pattern). `DashboardView` renders the zone. `NudgeCard` gained an optional `snoozeLabel` prop (briefing uses "Dismiss").
- **Persistence keys:** `action_briefing` (`{weekKey, ids[], dismissed?[]}`), `last_seen_update_version`. Both in the shared Postgres settings.
- Verified live on :5173: What's New card + 2 focus items (Amazon 109% / Personal Care 115%, real numbers), Review-spending deep-link → Budget→Transactions works, Dismiss buttons render, no console errors. Did NOT click Got it/Dismiss (shared DB) — left fresh for Scott.

## ✅ Also shipped this session (extended autonomous run — Scott: "keep rolling till we can get the code off this laptop to GitHub")
Commits on master, all tsc-clean + browser-verified, 264 tests:
- **`c658af2` Cash-flow calendar** — `src/utils/cashflowForecast.ts` + `CashflowCalendar.tsx`. Surfaces the previously-DORMANT `recurringDetector` (was built, unconsumed). Projects confident recurring OUTFLOWS forward 30d (calendar-accurate stepping; overdue dates roll forward), grouped by day. Collapsed DashSection "📅 Coming up · next 30 days" on dashboard. Display-only (useMemo off rawExpenses).
- **`bf288ef` Subscription radar** — `src/utils/subscriptionRadar.ts` + `SubscriptionRadar.tsx`. Same recurring candidates, ranked by MONTHLY-equivalent cost (Scott's real data: 31 charges, $8,530/mo). Collapsed DashSection "🔁 Subscriptions & recurring". DashboardView detects recurring ONCE, derives both calendar + radar.
- **`4add3e1` Distribution groundwork (Rock 4/5)** — `SETUP.md` + `npm run update` (`git pull --ff-only && npm install`). **SECRETS AUDIT PASSED CLEAN**: `.env.local`/Teller certs/keys are gitignored and were NEVER committed (only `.env.example`, empty placeholders; no connection string/token anywhere in history) → safe to push to a private repo. Schema auto-migrates on boot (`server/db-pool.ts` runs `runMigrations` on pool open, idempotent).
- Release notes for cash-flow + radar appended to the same `src/updates.ts` entry (What's New card, still undismissed for Scott).

## ⏭️ THE ONE REMAINING STEP TO SCOTT'S GOAL — HIS action (needs GitHub auth; `gh` not installed, no remote yet)
Iris is ready to leave the laptop. Scott runs (from `signal-app/`): install GitHub CLI → `gh auth login` → `gh repo create iris --private --source=. --remote=origin --push` (or manual `git remote add origin <url>` + `git push -u origin master`). Then on laptop 2 / Claire's: `git clone` → `npm install` → `.env.local` with the SAME `DATABASE_URL` → `npm run server`. **Data is already in cloud Supabase — nothing to migrate; "data off the laptop" is already true.** Full steps in `SETUP.md`.

## ✅ Rock 2 started this session (commit `7abc79b` on master, browser-verified)
- **Dropped h2h-lead-1/h2h-lead-3** from the achievements catalog (Scott's call: cooperative-only extends to badges, not just the fun-money box itself — the box/game stays, badging "whoever's ahead" doesn't). No unlocks existed yet — zero orphan risk. Catalog now **39 achievements** (bronze 15/silver 11/gold 8/platinum 5). Removed dead `funLead` helper.
- **Dedicated Achievements view + nav** — new `achievements` View (`src/views/AchievementsView.tsx`), sidebar nav entry with a new trophy icon (`Icons.trophy`), gated in `useEnabledModules.visibleViews` same tier as Budget (always-on). Dashboard's full Trophy Room `DashSection` replaced with a compact **teaser DataCard** (earned/total + "Next up" medallion) that links to the new page — celebration nudges for fresh unlocks still fire on the dashboard regardless of page.
- tsc clean, 264 tests unaffected, tier math verified correct in browser (5/8/11/15=39).

## ✅ Rock 2 cooperative achievements ALSO done this session (commit `2a142a3`) — turned out cheap
Contrary to the original design doc, NO new engine primitive was needed — `funMoneyStreaks`/`underBaseStreak` already derive per-month history live, so "both banked" is just `Math.min(...)` across existing per-person streaks. Added `household-machine-6` ("In Sync", platinum), `household-machine-12` ("Perfectly Aligned", platinum/secret), `synchronized-discipline` ("Locked In Together", gold — both banking + household under base, sustained together). **Bugfix found in passing:** `household-machine` (existing 3mo tier) was missing `forwardOnly` — a real gap vs. the locked rule; fixed free (no unlocks existed). Catalog now **42 achievements** (platinum 7/gold 9/silver 11/bronze 15). +6 tests (270 total), browser-verified tier math + tile rendering.

**Rock 2 is now essentially complete** for this pass — dropped competitive badges, dedicated view/nav, new coop achievements all shipped. Remaining Rock 2 items are lower-priority/deferred (per achievements-catalog.md "Deferred" list): comeback-kid generation counter, treat-yourself, first-week-explorer, stash-progress achievements, distinct-months-used — none couples-specific, can wait.

## ✅ Rock 2 fully closed + Rock 3 STARTED this session (commits `96fbef3`, `a99df64`)
- **`96fbef3` "Comeback after a week away"** — the pick-me-up trophy Scott specifically asked for ("that's always important to pick a person up"). Zero new tracking needed: `AppDataContext` already persisted `last_visit_at`/`prev_visit_at` (built for a "welcome back" nudge) — just plumbed the gap into `EngagementSignals.daysSinceLastVisit` + one forward-only one-shot achievement. Catalog now **43 achievements**. Rock 2 is DONE for this pass.
- **`a99df64` Rock 3 design polish, first slice.** Scott brought a detailed anti-box-monotony spec from Gemini and asked for Claude's honest review before building — see [[project-iris-design-direction]] "Rock 3 design polish pass" section for the full review (2 real conflicts flagged against locked decisions: serif font vs. Inter-everywhere SKIPPED; glow/neon vs. cyberpunk-retired → very-subtle-only). Implemented: de-boxed Iris's Take + StashesCard (soft well + editorial left accent, matches/extends the existing inner-text treatment), visible progress tracks + subtle green gradient (NOT neon), BudgetView top-4 tiles refined to bottom-border-only (they already had 90% of the ask built — bg tint/hover-lift/arrow/active-state all pre-existed), sidebar duplicate widget removed + active-row left indicator + one grounding divider. Skipped the broad grid rewrite (existing breakpoint grids already do the job). **Self-flagged tension to watch:** the new well/soft-well surfaces are LIGHTER than the deep-dark glass-card fill, which is close to an experiment this doc already recorded as rejected ("grey frost, killed the pop") — this time it's scoped to 2 sections for deliberate variety, not a universal re-lighten, and screenshots looked clean — but **Scott should eyeball these two sections live and veto if it drifts.**
- tsc clean, 274 tests unaffected (pure CSS/JSX for the design pass), browser-verified with screenshots throughout, no console errors.

## ✅ Rock 3 tooltips + tutorial DONE this session (commits `5f5c35d`, `53072d9`)
Scott confirmed the design-polish pass looked fine live (no revert needed — the "lighter well" self-flagged tension resolved itself as intentional variety, not a relapse). Confirmed no more Gemini rounds coming — future visual asks will be Scott's own. Moved straight to tooltips + tutorial to finish Rock 3.
- **`5f5c35d` Tooltip system** — new `InfoTooltip.tsx` (portal-based, escapes `overflow-hidden` ancestors via `getBoundingClientRect` + `createPortal` to `document.body`). Applied to 7 places: This Week's Focus, Safe to Spend, Living Under the Base, OTE over base, Coming Up (cash-flow calendar), Subscriptions & recurring, Achievements forward-only rule. **Real bug caught+fixed**: `DashSection`'s collapsible header was a native `<button>` — the new tooltip icon (also a button) landed nested inside it for 2 placements, invalid HTML + broke event handling. Fixed by swapping to the `div role="button" tabIndex={0}` pattern already used in BudgetView's tiles (added onKeyDown since divs don't get that free). Verified via direct fiber prop invocation (test-harness mouse/focus simulation didn't reach React's synthetic events, but the component logic itself was proven correct this way) + no console errors after the fix.
- **`53072d9` Tutorial rewrite** — the 5-step first-run walkthrough (`Tutorial.tsx`) existed but was STALE (old investment-framed copy, a nonexistent floating chat button) and had been fully suppressed behind a hardcoded `DEV_MODE=true` gate the whole time it existed. Rewrote all 5 steps for current reality; removed the DEV_MODE gate (confirmed it had no other use). **Seeded `tutorial_completed_Scott`/`Claire`=true so it doesn't retroactively surprise-pop** — done via the app's OWN settings API (`POST /api/settings/save` against the running preview server) after a raw-SQL attempt against the `collections` table was correctly BLOCKED by the permission classifier (wrong schema guess anyway — settings actually live in a separate `settings` table, columns `user_id/key/value/updated_at`, confirmed via `server/api-handlers/settings.ts`). Scott explicitly authorized the seed via AskUserQuestion before it ran.
- Both commits: tsc clean, 274 tests unaffected (pure UI/content, no logic changed).

**Rock 3 is now functionally complete** for this pass (repaint + tooltips + tutorial). Only future Rock 3 work is Scott's own ad hoc visual tweaks (not Gemini specs going forward).

## 🔀 MAJOR DIRECTION (2026-07-07): SELF-HOSTED, not a desktop installer. Auth being built.
Scott + Claude worked through distribution and landed on **self-hosted, not a packaged app**: one always-on host (Scott's 3D-printing laptop; later a micro-desktop) runs the existing `server/standalone.ts`; every device (Claire's laptop, phones) reaches it from a **browser via URL over Tailscale** — no per-device install, no Electron, no auto-updater (updates = redeploy the one host → refresh). The abandoned Electron scaffolding was removed (commit 070fc83). Data stays in cloud Supabase; the LLM/Gemini key lives IN the DB so it "folds over" automatically — the only thing a new host needs handed to it is the DATABASE_URL (+ optional Teller cert files, only on whichever host runs bank sync). Teller sync: one host holds the certs + syncs on a schedule; the DB already holds the per-bank access tokens, so other machines just read. Future: database-per-tenant ("invite Ryan & Molly" = each their own DB), built on the same auth foundation. Hosting = free via Tailscale (6-user free plan) on hardware Scott has; the "cost" is a machine staying on. Code-signing skipped (personal use).

**⚠️ THE LOAD-BEARING GATE: the API had ZERO server-side auth** (only safe because it bound to loopback). Confirmed by audit. So hosting REQUIRES real auth first — that's the current build, in 3 slices:

**✅ Slice ① DONE — auth backend (commit `3f18884`, dormant/safe).** Migration 0005 (auth_accounts w/ scrypt hashes + auth_sessions storing only SHA-256 of the cookie token). `server/api-handlers/auth-core.ts` (hash/verify, cookie httpOnly+SameSite+Secure-gated-on-x-forwarded-proto, DB sessions) + `auth.ts` endpoints (/api/auth/status,setup,login,logout,me). Login accounts are SEPARATE from the single household `user_id` — auth gates access, doesn't partition data (Scott+Claire share the budget). `requireContext` (the one chokepoint all data routes use) now validates the session — but **FIRST-RUN RULE: while zero accounts exist it allows access (behaves exactly like today, open on loopback), so this commit does NOT lock anyone out.** The gate engages the moment accounts are created. `standalone.ts` refuses a non-loopback (IRIS_LAN=1) bind until accounts exist. Verified against real DB WITHOUT creating accounts (migration applies, status/me/first-run-open correct). +10 unit tests, 284 total, tsc clean. **⚠️ DO NOT create accounts via /api/auth/setup until Slice ② UI exists — doing so engages the gate and would lock Scott out (he'd have no login screen + no known password).** setup is name-agnostic (creates whatever usernames you type, not hardcoded Scott/Claire).

**✅ Slice ② DONE — auth frontend (commit `3014fe2`).** `authClient.ts` + `AuthScreens.tsx` (ConnectScreen for a fresh host's DB string / SetupScreen create-accounts, any names / LoginScreen) + `AuthGate.tsx` state machine wrapping the app. `App.tsx` rewritten: ripped out the auth_users/PIN/needsLock logic, now `<AuthGate>{(user,logout)=><AuthedApp/>}`. activeUser = logged-in display name. Sidebar "Lock" = real logout. `main.tsx` no_credential now renders the app (AuthGate ConnectScreen) instead of a raw screen. LockScreen.tsx deleted. Browser-verified the SetupScreen renders against the real DB WITHOUT creating accounts (no lockout — first-run is Scott's, with his own passwords). tsc clean.

**✅ Slice ③ DONE — hosting docs + extras (commit `b0d8428`, pushed).** .env.example documents PORT + IRIS_LAN (+ safety note). SETUP.md got a first-run section + a full **Tailscale runbook** (install Tailscale → `IRIS_LAN=1` → `tailscale serve --bg 5173` for auto-HTTPS → open ts.net URL on any device). Iris logo (top-left) now always opens Ask-Iris chat. subscriptionRadar rounding nit fixed (totals from raw, round once). **In-app "Update Iris" button DONE (commit `f1ef45b`, pushed):** Settings → Updates → button hits POST /api/update (server/api-handlers/update.ts) which git-pulls+installs+rebuilds on the host, reports up-to-date / changed-count / restart-needed (server-file change → restart; frontend-only → just refresh). Gated behind requireContext (login required), fixed commands only. Failed build leaves old dist/ intact.

## ⏸️ IN-FLIGHT AT SESSION END (2026-07-07) — RESUME HERE
Everything above is built, committed, and PUSHED to github.com/Lonesoul65666/iris (latest commit `d5f2aba`, tsc clean, 284 tests). **Scott is mid-bring-up of the always-on HOST on his OTHER computer and hit "some issues" (unspecified — ASK him what broke first).** State:
- New machine has NO Claude and doesn't use the `C:\Claude` path. Scott chose folder layout: **`C:\ProjectIris\iris\`** = the cloned app, **`C:\ProjectIris\certs\`** = the 2 Teller cert files.
- **KEY GOTCHA (already bit us):** the git repo root IS the app — a clone of `iris` has `package.json`/`src`/`server` at top level, **NO `signal-app` subfolder**. So the app dir is `C:\ProjectIris\iris` directly; run all `npm` commands there. (SETUP.md was fixed for this in `d5f2aba`.)
- Prereqs on the new box: Node ≥22.6 + git (`node -v`, `git --version`; install from nodejs.org / git-scm.com if missing). Nothing else — no per-machine installer; it's a plain clone+run.
- Bring-up sequence he's working through: `cd C:\ProjectIris\iris` → `npm install` → `copy .env.example .env.local` → edit `.env.local` (set `DATABASE_URL` = same value as this machine's `.env.local`; `TELLER_CERT_PATH=C:\ProjectIris\certs\certificate.pem`, `TELLER_KEY_PATH=C:\ProjectIris\certs\private_key.pem`, `TELLER_ENV=development`) → `npm run server` → open printed localhost:5173 → **first-run "Set up Iris"** (create accounts, ANY names, his own passwords) → log in.
- **Teller certs source (this machine):** `C:\Claude\projects\Teller\certificate.pem` + `private_key.pem` — must be hand-copied (privately; private_key.pem is a secret) to the host's `C:\ProjectIris\certs\`. Only the always-on host needs them (it syncs; other devices just read).
- After it runs: Tailscale for phone/remote access (Part 6, not yet done) — install Tailscale on host+phone, `IRIS_LAN=1`, `tailscale serve --bg 5173` (auto-HTTPS), open the ts.net URL. Full runbook in SETUP.md.

**⚠️ Once accounts are created the auth gate is LIVE.** If login ever breaks, escape hatch = run on loopback (default bind) + the accounts/sessions live in the `auth_accounts`/`auth_sessions` tables. The server refuses a wide (IRIS_LAN) bind until accounts exist.

## ⏭️ REMAINING (after the host is up)
Optional/next: meet-in-middle tuning (Rock 1 leftover); investments/equity still deferred (PHASE_1_LOCK); future database-per-tenant ("invite Ryan & Molly"). All committed+pushed; the app on THIS machine still runs open-on-loopback (no accounts here) so Scott's dev instance is unaffected.

Pre-hosting light audit (3 Sonnet agents) came back clean: money-math honest (one cosmetic rounding nit), feature wiring clean, secrets clean (nothing sensitive ever committed). Two untracked `public/*-mockup.html` Gemini experiments would get bundled into a build — flagged to Scott, harmless.

## ⏭️ NEXT — remaining rocks (scope each with Scott first, cost-conscious)
- **Rock 1 leftover:** meet-in-the-middle tuning + fun-money flourishes; optional deeper "you ignored this 3×" action-item learning (piping still DEFERRED per [[project-iris-dynamic-action-items]]). Possible refinement: subscription radar currently includes fixed bills + the FID investing transfer (it's all recurring outflow) — Scott may later want a subscriptions-only vs fixed-bills split.
- **Rock 3** — visual design: **Scott drives the subjective repaint with Gemini** ("everything in the same style box"); Claude does the OBJECTIVE parts (tooltips on cards/graphs, first-run tutorial — a `Tutorial` component already exists in `src/components/Tutorial/`, already wired in App.tsx, worth checking what it currently covers before building more).
- **Rock 4/5 finish** — after Scott's push: verify a real second-machine bring-up; optional self-update polish; concurrent-write conflict still DEFERRED.

## Gotchas
- Bash cwd resets to `C:\Claude` — `cd /c/Claude/projects/signal/signal-app` first.
- Untracked `scripts/*.mjs` probes + `public/*-mockup.html` (Gemini redesign experiments) are one-offs — do NOT commit them; stage feature files explicitly.
- vitest include glob is `src/utils/__tests__/**` + `server/__tests__/**` — put unit tests there (app-root `src/__tests__` is NOT picked up).
