---
name: project-iris-backlog
description: "Iris prioritized backlog — the 3-bucket build list Scott wants surfaced whenever he asks to 'continue Iris'. Bucket 1 = do-next (important + cool), Bucket 2 = future-state, Bucket 3 = pie-in-the-sky. Read this + the latest handoff at the start of any Iris session."
metadata: 
  node_type: memory
  type: project
  originSessionId: b5b6daf6-8dcb-4cc3-8b06-70219bf8d84b
  modified: 2026-08-17T19:16:13.797Z
---

# Iris backlog — prioritized 3-bucket build list

**When Scott says "continue Iris," surface this list (in priority order) alongside the latest handoff so he can pick.** Scott built these buckets himself (2026-07-18): 1 = important/should-do + cool near-term, 2 = future-state improvements, 3 = pie-in-the-sky.

## 🔥 Bucket 0 — DATA-INTEGRITY QUEUE (added 2026-08-12, jumps the line)

Found by Scott at the July→August rollover. These are **correctness bugs in the money math and the celebration layer**, and they come before the rail/quest work because **the Quest Engine builds on this data and would inherit every one of them**. Full spec with code pointers: `docs/data-integrity-queue-2026-08.md`. Detail in [[project-iris-handoff-2026-08-12]].

1. 🔴 **Achievements/Moments fire too early — no settle lag.** `isCompleteMonth` is a pure string compare (`src/utils/transactionAnalysis.ts:14`) → at 00:00 on the 1st the prior month is "final," but Plaid settles 1–3+ days late. **Corrupts stored state:** Moments are idempotent on `(type,periodKey,person)` and persist `magnitude`, so an early fire writes a wrong number that the key then blocks re-firing. Fix = settle lag (display live, celebrate/log after close) + an amend policy for late data.
2a. ✅ **PARTIAL FIX 2026-08-13 (commit `94e4284`) — refunds are now recategorizable.** The category picker was gated on `transactionType === 'expense'`, so refund rows had NO picker — even though **a refund nets against its category, so the category IS the mechanism.** Scott's Citi dispute credits arrive described only as `CITIBANK CONDITIONAL CREDIT FOR DISPUTE` (nothing identifies the merchant) → classifier drops them in `other`. Real damage found in his data: Alivemoment charges hit `healthcare`/`subscriptions` while all three credits ($106.24 Aug 10, $106.24 Jun 14, $94.56 Apr 18) netted out of `other` — **month totals right, every bucket wrong**, and `other` has no budget bucket so the offsetting negative was invisible. Refund rows deliberately do NOT write a merchant mapping (generic descriptor would tag all future disputes). ⚠️ NOT screen-verified (dev session expired). **Still wanted: an auto-matcher** — the credits pair perfectly with their charge by SAME AMOUNT + SAME DAY, so a post-import pass could inherit the charge's category automatically instead of Scott hand-fixing each one.
2. ✅ **BUILT + PUSHED 2026-08-13 (commit `38412fb`) — dispute lifecycle + the "needs your call" queue.** ⚠️ **NOT screen-verified** (dev session expired; Claude won't enter passwords) — math + persistence verified, UI not. **Scott's locked decisions:** ① charge is **EXCLUDED from spend while OPEN** (not counted-until-proven) — "I don't believe I owe this," and it stops eating a category (Links was eating Claire's fun money); ② **Iris OFFERS the matching credit, Scott confirms** — he explicitly did NOT want pattern matching doing it silently; amount+date is reliable here because **every one of his Citi dispute credits posts the SAME DAY for the exact amount**; ③ **recall is EVENT-DRIVEN — the credit landing IS the reminder** ("a $34.99 credit landed Aug 11 that matches this — mark won?"), with a 14-day staleness flag only as backstop. That was his actual question: "what's my reminder recourse outside of digging through my transactions." **States:** `open` → excluded+badged+listed; `won` → stays excluded AND offsetting credit suppressed; `lost` → normal spend. **⚠️ THE LOAD-BEARING TRAP (tested both ways):** excluding the charge while ALSO letting the credit net invents money (−$34.99 phantom savings) — the credit link is what prevents it, so `resolveDisputeWon` returns BOTH patches and callers must apply both. Disputed money is **totalled not hidden** (`MonthlySpending.totalDisputed`). **Built as ONE surface with the cash-out queue** because both are "a transaction needing a human decision, surfaced until made" — separate builds = same queue twice. Files: `utils/disputes.ts` (pure), `components/Budget/NeedsYourCall.tsx`, dispute badge/button in `ExpenseManager`, mounted in `BudgetView` above the pots. All 6 dispute fields added to the sync preserve list on all 3 upsert sites (the bank doesn't know you're fighting a charge). 23 tests, 393 total.
2b. 🔴 **(superseded above — original entry)** No dispute state on transactions. (Live need as of 2026-08-13: Links Car Wash $34.99 on 2026-08-08, flagged by the watchdog as billed-after-cancel, dispute pending "a few days." Currently sits in `fun_wife` — **so a disputed charge is eating Claire's fun-money allowance**, a concrete reason this matters.) `Expense` (`src/types/budget.ts:70-91`) has `reimbursementStatus` but nothing for disputes. Scott's CC dispute was correctly *caught* by the watchdog but couldn't be *marked*. Needs a `disputeStatus` lifecycle + scorecard treatment (open→exclude visibly; won→exclude AND suppress the matching credit row or the month double-counts; lost→normal spend).
3. ✅ **FIXED + PUSHED 2026-08-13 (commit `e8378d2`) — host update pending.** One cash-out concept: new `isCashOut()` routes **ATM withdrawals + Cash App sends → `atm_cash`** ("ATM / Cash", Scott's own custom category), always `expense`. Was: the SAME PAI ATM withdrawal classified `atm_cash` in Mar, `travel_personal` in Jun, uncounted `transfer` in Aug; and **two $160 Cash App sends (Jun 23, Jul 24) were never counted at all.** ATM inflows (rebates, fee waivers, deposits) untouched. Bonus: an ATM pull from a savings bucket now counts AND trips the unexpected-outflow tripwire it was silently bypassing. The feared retro-rescoring turned out SAFE — `surplusVsBase` and `banked` use TOTAL spend, not the operating/reserve split (`savingsScorecard.ts:141-142`), so no Moment can flip from a recategorization. Venmo/Zelle rules left as-is (already `expense`).
4. ✅ **FIXED + PUSHED 2026-08-13 (commit `e8378d2`) — host update pending.** Root cause below. Fix = (a) `pfcToType` splits `LOAN_PAYMENTS`: credit-card → still skipped, mortgage/car/student → falls through to the classifier; (b) new **`isDefiniteSpend()`** allowlist in `transactionCategorize.ts`, consulted BEFORE `CHECKING_SKIP_TYPES` in `teller-map.ts`, so no future feed reclassification can delete a bill. Deliberately an allowlist — `classifyBankTransaction` defaults unmatched rows to `expense`, so trusting it wholesale would neuter the double-count guard. Data backfilled (`scripts/backfill-cashout-and-mortgage.mjs`, 22 rows, backup written): Jun +$160, Jul +$160, Aug +$3,407.84. Verified live: Housing went `$0 / $3,205 UNTOUCHED` → **`$3,204 / $3,205 ON TRACK`**. ⚠️ 4 `travel_personal` ATM rows (May ×2 $407.90, Jun ×2 $197.50) HELD BACK — that attribution may be deliberate; `--include-travel` folds them in. **Original diagnosis:** It's (a), and the example is THE MORTGAGE.** `WF HOME MTG DES:AUTO PAY` $3,203.84 arrived on **2026-08-03 tagged `transactionType: 'transfer'`** — for the 11 straight months Sep 2025→Jul 2026 the identical payment came in as `'expense'`. `isRealExpense()` (`transactionAnalysis.ts:33`) drops non-expense types, so **August's single biggest bill is invisible to spend**: Pulse shows Housing `$0 / $3,205 UNTOUCHED`, and August's real-expense total is $3,978.54 — understating actual spend by ~45%. Safe-to-Spend is NOT affected (fixed lanes count `max(budget, actual)`), which is why nothing looked wrong. **But the month-end "you saved this much" IS overstated by ~$3,204** — very likely why Scott says he can't make sense of that number. Fix shape: a FIXED-lane bill (housing/mortgage/loan) must count as spend even when Plaid calls it a transfer — Plaid sees bank→lender and says "transfer," the budget says "the mortgage." Genuine savings transfers ARE correctly excluded ($2,000 May / $3,000 Jun / $1,000 Jul), so the mechanism is right, it just misfires on bills. Needs mapper fix + an August backfill or re-pull. Probes: `scripts/probe-mortgage-flow.mjs`, `scripts/probe-aug-flows.mjs`.
5. 🟢 "VA" capitalization in Spend-by-account (was Bucket 1 #5; likely `src/utils/txDisplay.ts:35-41`).
7. ✅ **FIXED 2026-08-13 — Have-To/Want-To pacing paced on fractional calendar time**, so every pot read "behind" with a required that climbed daily. Now counts discrete commit months. Full detail + Scott's locked stash model in [[project-iris-handoff-2026-08-13]].
8. 🚫 **DECIDED AGAINST 2026-08-13 (Scott): stashes do NOT auto-draw-down.** Found that no pot links a category (`categories: []` on all six) so nothing ever draws them down; Scott's call — **"we definitely don't want to draw down"**. Pots stay pure accumulators; category-linked automatic draw-down is off the table. ⚠️ **Known consequence, unresolved:** a pot therefore still reads fully funded after its bill is paid, and the legacy `RESERVE_ALLOCATIONS` ($2,000) keep driving the reserve lane instead of the stash plan. If that ever needs solving, the shape Scott would likely accept is an **explicit "paid this bill from this pot"** action, not category inference.
9. 🟡 **NEW (found 2026-08-13, NOT fixed): `VariableSurplusCard:118` inflates "free to deploy"** — credits `totalReserveSetAside() × monthsElapsed` ($2,000 × 8 = $16,000) against the $2,403 actually committed, overstating free cash by up to ~$13.6k.
6. 🟡 Month-rollover freshness — candidate rung-1 win: the rail says "July is closing, holding the scorecard until charges settle," turning a data constraint into an honest narrative beat.

10. ✅ **FIXED + PUSHED 2026-08-13 (commit `137f9f3`) — user can now RECLASSIFY a transfer, and it sticks.** Scott's ask: "IF I need to re-classify cashapps or ATM or Zelle, make sure those are not ignored." He couldn't, twice over: (a) the Type column was a **read-only badge** and the category picker only rendered for `transactionType === 'expense'`, so a feed-tagged transfer was greyed out with NO type control and NO category control — **97 of his 2,174 rows were un-reclassifiable except by SQL**; (b) the sync upsert preserved category/work/reimbursement/notes/recurring/incomeSubtype/spender but **NOT `transactionType`/`flow`** — the one field deciding whether a row counts at all was the one the bank could silently take back. Fix: Type is a `<select>` (expense/transfer/investment/refund) which reveals the category picker; new **`typeOverride`** flag set only on a hand edit, and the ON CONFLICT clause preserves type/flow **only when it's true** — untouched rows stay mapper-owned deliberately, so classifier fixes still re-correct history (an unconditional preserve would freeze old mistakes). All 3 upsert sites (plaid.ts, teller.ts ×2). Verified against real Postgres: `scripts/verify-type-override.mjs`. ⚠️ **Gotcha for future tests: a UI save (`saveExpense` → `expenses.ts:200`) is a PLAIN `data = EXCLUDED.data` replace; only SYNC writes use the preserve clause.** Modelling a UI edit with the sync SQL gives a false failure.
13. ✅ **FIXED + PUSHED 2026-08-13 (commit `c2dd15b`) — the Pulse was hiding $76,948 of real spend AND overstating "still free".** Root cause: it could only render categories WITH a budget bucket, and it filtered reserve lanes out entirely — so unbudgeted/lumpy spend was invisible **and missing from `totalActual`**. Since `freeOfBase = watermark − totalActual − committed`, **Jan 2026 read ~$2.1k "still free" while the month was $14.1k OVER base.** ⚠️ **IMPORTANT NUANCE: the spend MATH was always correct** (`totalExpenses`, scorecard, `banked`, `surplusVsBase` all counted every dollar) — this was purely a display/total bug on one card. Never tell Scott his numbers were "wrong"; the card was incomplete. Per-month gap: Dec $7,015 / **Jan $16,276** / **Feb $17,619** / Apr $14,366. By category: travel_personal $50,558, taxes $16,724, atm_cash $4,050, car_insurance $3,291, credi_card_memberships $872. **Fix:** synthesize a budget-0 row for any category with spend but no bucket (`unbudgetedRows` in BudgetView); keep reserve rows but force displayed budget to 0 so they classify `nobudget` (neutral pill — preserves "a $14k tax payment must never read OVER" while still counting); projection counts lumpy/uncapped at ACTUAL (never extrapolated — $13k tax would project ×2.7 — but never skipped either). Verified live: Aug total $5,700→**$6,960** = exactly the spend math's $6,960.27; still-free $8,679→$7,419; trending $11,908→$13,168. All 12 months reconcile. Probes: `scripts/probe-pulse-gap.mjs`, `probe-pulse-coverage.mjs`.
12. 🟡 **(mostly addressed by #13) ORPHAN CATEGORIES — spend counted in totals but with no budget bucket, so invisible in the Pulse/budget grid.** `scripts/probe-refunds-atm.mjs` lists them: **travel_personal $50,557.79** (71 rows — reserve lane, may be by design), **atm_cash $4,049.59** (23 rows — the new cash-out home has NO bucket!), **car_insurance $3,291.00**, **other $1,189.74**, **credi_card_memberships $872.07** (⚠️ note the TYPO in the category id), electronics $146.10, alcohol $83.76, debt $18.68, bills_misc $16.00. ⚠️ Adding buckets disturbs the zero-based balance (21 buckets $13,397 + 6 pots $2,403 = exactly $15,800) — a $0-budget bucket makes the spend visible without changing the plan. There's an existing `scripts/scan-orphan-buckets.mjs`.
11. 🟡 **Scott's model, confirmed as correct and worth keeping straight (2026-08-13):** account-to-account transfers are visible in the dashboard but are NOT money in/out; **credit card payments are just realizing money already spent on credit** (the purchases were counted when they posted) — so both must stay excluded from spend. Cash out (ATM/Cash App) is the opposite: the money is gone, so it counts.

## 🎯 Bucket 1 — Do next (important + cool, near-term)
1. ~~**Milestone celebrations**~~ — ✅ **SHIPPED + PUSHED 2026-07-19** (commit `e246936` on origin/master). Net-worth milestone ladder $250k→$10M (`celebrationStyle: 'takeover'`, forward-only vs frozen baseline so already-past levels grandfather). Full-screen `CelebrationOverlay` (queue drains oldest-first) + **trophy REPLAY** (every earned wall tile clickable → reliving w/ unlock date; the couples "you saw it, I didn't" case, Scott's ask). Modal hygiene (Esc/autofocus/`prefers-reduced-motion`), brief confetti (live unlock only), "Next up: $X →" line, and **Iris's FIRST SOUND** — synthesized Web-Audio chime (no asset file), opt-out toggle in Settings→Preferences. 323 tests. **⚠️ KNOWN GAP flagged in commit + item #2/#3 below.**
2. **Coinbase → net worth.** Surface crypto totals via **Coinbase's own API** (Plaid doesn't do Coinbase holdings). Same lightweight "expose the total in total wealth" pattern used for the Fidelity accounts. **⚠️ MUST re-baseline the net-worth milestone floor when the contributing-account set changes** — else connecting a new account inflates net worth and false-fires a milestone celebration for money already held (baseline is frozen at first-run). This guard was deliberately deferred to this connector work.
3. **Robinhood → net worth.** Scott has Robinhood investments; Plaid supports Robinhood — pull via the same investments path. **Same re-baseline guard as #2 applies.**
4. **Multi-line / toggleable investment chart.** Separate on/off-able trend lines (cash / Fidelity / Coinbase / Robinhood) with checkboxes, so Scott can watch each pillar move independently. He's genuinely excited about this one. NOTE: historical per-line data will be flat for the pre-tracking stretch (same "no history from Plaid" limit); forward is accurate.
5. **"VA" capitalization fix** in the Spend-by-account section. Small polish — a label rendering lowercase. PENDING the exact label from Scott (he couldn't pin which one when asked).
6. ✅ **FIXED 2026-07-19 (commit `f2a1470`, pushed) — "Ask Iris" was blind to real data.** Root cause: `buildPortfolioContext` in `src/services/gemini.ts` fed the LLM only a budget PLAN + averages — no per-month actuals. The phantom "$13,396" was literally the **sum of the 21 budget buckets' monthlyBudget = $13,397** (confirmed by DB query), which it mislabeled as "your budget"; the real frame is the **$15,800 guaranteed base**, which the chat never saw. Fix: added a transaction-grounded `performance` block to `BudgetContext` (guaranteed base, monthsUnderBase, cumulativeBanked, trend, per-month actuals table, current in-progress month w/ MTD+buffer+safe-to-spend+days-left), populated from `computeScorecard(rawExpenses)` + `safeToSpend` in `sendMessage` (AppDataContext), serialized it BEFORE the category budget, and relabeled the bucket sum "category budget (planning detail — NOT the income frame)". **One builder covers all providers** (Gemini native + router for Claude/OpenAI/Ollama). Verified LIVE (real Gemini): "how did I do in June?" now → "$14,371 spent, $1,429 under your $15,800 base, banked $2,911" (was "import your transactions"). 4 regression tests in `gemini-context.test.ts`.

## 🔭 Bucket 2 — Future-state improvements
1. **Full Plaid Investments → Portfolio (holdings-level).** Individual positions per account (not just totals), replacing the manual Fidelity CSVs, feeding X-Ray/rebalance/allocation. The lightweight account-TOTALS version already shipped (2026-07-18); this is the deep version. Uses Plaid's `investments` product (we currently only request `transactions`).
2. **Mobile / responsive layout.** Works on Android (home-screen app, reachable anywhere via the funnel) but "looks like shit" — cramped, sometimes needs landscape. Possibly a device-aware layout or a dedicated mobile view. Scott: functional but not comfortable.
3. **Server-side daily snapshots.** So the net-worth trend line ticks daily even when the app is closed. Today snapshots are client-generated on open; this would let the 12h host timer keep net-worth *history* current (pairs with a server-side balance refresh). Decided NOT worth building yet (2026-07-18) since net worth is only viewed when open — revisit if it matters.
4. **The "big swing" (north-star).** Adult money gamification + Proactive Iris nudge/announcer engine — see [[project-iris-gamification-roadmap]]. Tier-2 of the locked product roadmap.
   - **MOMENTS (✅ phases 1–3 SHIPPED + PUSHED 2026-07-19, commit `f568c76` on origin/master; phases 4–5 remain):** two separate-but-wired concepts — **Achievements** (permanent, one-and-done, tiered; the wall) vs **Moments** (REPEATABLE monthly wins). v1 catalog (all from existing data, zero new tracking): **Beat the Clock** (month under $15,800 base), **Both Banked** (both partners under fun allowance same month), **Held the Line** (solo fun-money win), **Goal Crushed** (retired a Want-To), **Restraint Dividend** (skimmed fun→savings). Moments live as a tally/streak (NOT wall clutter) and *roll up into* Achievements (beat-the-clock ×N → trophy). Data model: `moments_log` settings JSON blob, idempotent on `(type, periodKey, person)`, forward-only (past months seed the tally COUNT but don't retro-celebrate). Completed months log+celebrate; **current month = a LIVE QUEST shown in-progress with urgency/countdown** ("$2,770 buffer, 11 days left, hold the line"). Celebration routing: routine Moment = quiet NudgeCard; milestone/streak-completing Moment = escalate to the `CelebrationOverlay` takeover. Phases: (1) engine+log+tally, (2) celebration routing, (3) live current-month quest, (4) Moments collection grid, (5) count-based achievements. Spec: `docs/moments-spec.md`.
   - **⭐ THE AI QUEST ENGINE (next big layer AFTER Moments — the real "draw," Scott 2026-07-19):** This is the crystallization of Proactive Iris + [[project-iris-dynamic-action-items]]. Insight: **in Pokémon the quests come TO you** — the game hands you bite-sized, achievable, REWARDED tasks on a cadence, giving a reason to log in. AI is uniquely able to generate personalized, data-grounded quests a static budget can't ("Iris noticed dining crept up → this week's quest: hold dining to $X"). **Moments is the REWARD SUBSTRATE quests produce; that's why Moments is built first.** PoGo mechanics to mirror (researched): Field Research = weekly AI quests; Research Breakthrough = monthly stamp-box (KEY: stamps DON'T reset on a missed day = rewards consistency-not-grinding = already matches Iris's "Glad You're Back" life-happens ethos); Special Research = multi-month "sagas" (e.g. "Emergency Fund Saga," staged); Buddy = Iris herself, leveling with you; Pokédex = the Moments grid. Scott's framing: app started engineer-driven/surgical (the analytics he needs), gamification going on now (for him+Claire), the AI-quest layer is the missing draw. NOT far-fetched — it IS the locked north star.
     - **⭐⭐ THE QUEST-ENGINE NORTH STAR (Scott, 2026-07-19): "money is the mechanic, not the message."** Claire genuinely HATES talking about money — making it fun for HER is the entire point. Make the money INVISIBLE and the game VISIBLE: verbs are play-verbs (catch/level/co-op/grow/keep-the-streak), NEVER finance-verbs (budget/review/track/analyze). She's not "reviewing spending," she's "catching the thing before midnight." Design test for EVERY quest/feature: *"Would Claire, who hates money-talk, want to poke at this?"* If it smells like a chore, it's dead. Cadence = optional-but-rewarding ("a reason to poke at it before midnight"), NEVER a nag. Co-op AGAINST an external challenge ("fight the gym together"), never each other; either partner finishes a shared quest async. **Full brief: `docs/gamification-vision.md` + `docs/iris-project-state.md` (also copied to `C:\Users\ScottDeluke\Documents\Iris-Context\` for dropping into Gemini). ARCHITECTURE SEQUENCING: Moments engine = the ROOT (done, source-agnostic — quests are a NEW producer feeding the same tallies, they compose, no rework). Moments phase 5 (count-achievements) = safe anytime. Moments phase 4 (collection GRID) = DEFER until Quest Engine designed (it's the display case for ALL wins incl. quest completions; build once). Quest Engine needs a TOOL-CALLING backbone (LLM asks Iris for computed comparisons, Iris does the math, LLM narrates) — same backbone unlocks sophisticated comparative analysis; build once, used twice.**
     - **⚠️ ORDERING REVISED 2026-08-12 — backbone is NO LONGER first.** New order: **(1) Iris voice-rail + persona rewrite → (2) quests → (3) backbone.** Backbone-first meant *guessing* the tool surface. Its real job isn't general analytics (that's "rung 3," mostly already built in the views) — it's **answering follow-ups about a thing Iris just said** ("rung 2"), which is only enumerable once real quests exist. Navigation-by-chat lands here too, as a `navigate_to` *tool*, not a throwaway directive parser. Also locked: the **3-rung engagement ladder** (no mode switcher, ever), the **rail** (what's persistent is Iris *speaking*, not a box *waiting* — a blank composer is a work surface that violates "she claims, never works"), the **mission sentence**, **dollar→tangible translation** (source from real Want-To stashes, real math), **Iris's hard-time rules** (tease the anomaly not the identity; target self/household NEVER the partner; per-person dial; private to its recipient), the **"no good excuse" mechanic** (Iris asks first, ribbing earned by silence not by the spend), the resulting need for **Iris MEMORY** (anomaly→explanation→date), and the guardrail **"the plot is computed, the prose is generated."** Full design: `docs/iris-front-and-center-design.md`; summary in [[project-iris-handoff-2026-08-12]].
     - **REAL-STAKES DESIGN (Scott riff 2026-07-19, key decisions):** ① **Tie quests to REAL money, but as REDIRECTION not loss** — miss a monthly quest → your rollover fun money **sweeps into savings** instead of rolling over as spendable. Worst-case of "failing" = you saved money; incentive aligned with outcome. THIS is what makes real stakes non-evil (vs fake points). ② **DANGER LINE — never cross-partner punishment.** One partner's miss must NEVER cost the OTHER partner (resentment/control engine = opposite of cooperative fun). Rule: shared quests = both-win-or-sweep-to-savings; personal quests = each stake self-contained (your miss redirects YOUR money); a team BONUS for both hitting personal+shared is good — reward-for-together yes, punish-your-partner no. ③ **Stakes at MONTHLY cadence, not daily.** Daily = presence-rewarded, never absence-punished (preserves the anti-guilt "Glad You're Back" soul); the money-redirect resolves gently once a month. ④ **The PET/companion = reward sink** (PoGo Buddy) — quests/Moments earn things that feed/evolve it. Cheese lives ENTIRELY in the art (deferred to design pass); consider adult-appropriate ABSTRACT growth (money tree/garden, a city you build, a constellation filling in) over a literal cartoon creature — mock both. Moments stays the substrate all of this rewards against.
     - **⑤ THE CARROT / REWARD side (Scott riff 2026-07-19, mirror of the redirect penalty):** wins → **SELF-FUNDED real rewards drawn from money actually saved** — never phantom money; Iris only offers a treat it can SOURCE the dollars for ("you banked $150 → that funds the dinner"). **The reward's real job = PERMISSION TO ENJOY without guilt** — the guilt-tax on spending is what makes money suck for disciplined savers; a system that says "you earned this, here's where it comes from, spend it clean" removes it. This is the emotional payoff the app was missing. Prefer **EXPERIENCES tied to existing Want-To stashes** (massage/dinner/the wishlist thing) over raw "+$50"; a won quest can fast-track a Want-To. **Iris SIZES and SOURCES it** (Proactive Iris being useful, not cheesy). **Couple reward = shared win funds a shared treat (date night)** — the GOOD positive-cross-partner inverse of the ruled-out punishment. Penalty stays: fail-as-a-couple = fail-the-budget (natural, self-contained). **MISSION (Scott's thesis):** money is a top source of couple conflict/divorce; make money not suck, cooperatively — if it works for Scott+Claire it could genuinely help couples. That's the north-star emotional core.

## 🌌 Bucket 3 — Pie in the sky
1. **Database-per-tenant** — "invite Ryan & Molly": each household its own DB, built on the auth foundation.
2. **True market-adjusted historical reconstruction** of investment values — accurate backfilled curves (vs. the flat +$441k estimate we used 2026-07-18). Needs holdings + historical prices; heavy.
3. **Per-holding performance analytics** / deeper investment intelligence (how each position tracks up/down/flat).
4. **Longer-term catch-up features** — FI projection, subscription-vs-fixed-bill split in the radar, debt tools (deferred in the gamification roadmap tier-3).

## 🔐 Operational / security facts (don't re-flag)
- **Supabase Data API (PostgREST) DISABLED 2026-07-19** (Project Settings → API → "Enable Data API" OFF). Iris connects to Postgres via direct `DATABASE_URL` only (role `postgres`, bypasses RLS), so it never used the REST API. Disabling it closed all 12 Supabase Security Advisor findings (11 "RLS Disabled in Public" incl. connectors/auth_accounts/auth_sessions + 1 "Sensitive Columns Exposed" on connectors). **The RLS-off warnings are now moot — do NOT re-flag them; there's no public API exposing those tables.** If anything ever needs Supabase client-lib/PostgREST, that's the toggle to revisit (but Iris's architecture is connection-string-only by design).
- **Host bind gotcha (hit 2026-07-19):** the standalone server loads env via `node --env-file=.env.local` (RELATIVE path) — MUST launch from the app root (`C:\ProjectIris\iris`) or `DATABASE_URL` won't load → app shows the connection-string prompt + `[iris] REFUSING LAN bind: could not verify accounts` → binds loopback → LAN/Tailscale devices time out. Fix = relaunch from app root (`npm run server`); ensure the Startup-folder launcher `cd`s into the app root first. DB + accounts were fine; it was purely env-not-loaded. Resolved by reboot.

## 🚫 Decided AGAINST (don't re-litigate)
- **Plaid webhooks.** Considered 2026-07-13; rejected. Marginal freshness gain for a personal 2-person app, more surface area to maintain, and the 12h timer + on-open is the terminal design. Would only revisit at scale (which never applies here).
- **Manual CSV as the primary intake** — replaced by Plaid; "defeats the purpose" per Scott.
