---
name: project-iris-handoff-2026-07-04
description: "Iris handoff 2026-07-04 — big Dashboard + Budget polish pass (violet paint already live), then the \"Make Every Dolla Holla\" zero-based/commit build STARTED (spec v2, chunks A+B shipped, C/D/E pending). Caught + fixed a stash data-loss bug. READ FIRST."
metadata: 
  node_type: memory
  type: project
  originSessionId: b068b670-813b-4a1f-a59f-c1e26868e1e2
---

# Iris handoff — 2026-07-04 (Dashboard+Budget polish, then Make-Every-Dolla-Holla build A+B)

**MOST RECENT. Read first** (supersedes 2026-07-03). Branch `overnight-polish-2026-06-11`, repo `C:\Claude\projects\signal\signal-app`, detached vite :5173. **~80 commits over master.** tsc clean · 126/126 tests throughout. Long overnight session, all browser-verified in Scott's real Chrome. Reserve/stash math is delicate — see the chunk-C warning.

## Shipped this session (all committed, verified)
**Dashboard polish:** real **Update** button under a larger greeting (was a tiny icon); net-worth chart got date-on-hover + `1M/3M/6M/1Y/All` zoom toggle + **artifact-trim** (auto-drops the one-time $388k→$546k data-entry jump, starts a day later so the line climbs; killed the fake +40.69% → real ~+0.4%); **removed the confusing "Cash flow this month" card**; **account balances** now headline each "Spend by account" card (3 BoA balances real; Citi/CapOne show "balance not synced" — their connectors import txns but no balance, a server-side gap); **action-items chip deep-links to the Budget Action Items tab** (was dropping on Overview — the "phantom pending" was just mis-routing; the item is real: backdoor-roth tip). Notices card = REAL (insightsEngine, data-driven) but PAUSED (dismiss/accept + weekly cadence = Phase-2 intelligence, queued in `docs/post-phase-1-backlog.md`).

**Budget polish:** Safe-to-Spend breakdown made legible (bigger, take-home green, deduction amounts white, labels violet) + per-day line pops green; **Money Map = "prism"** — decomposed the lumped "Everyday" slice into top-3 spend categories + "Other" (Housing/Groceries/etc → investing → reserves → Free), one header (killed the double-$15,800); **Budget Pulse renamed → "Make Every Dolla Holla"** (big violet title) + status chips redesigned into matching rounded buttons.

**Coat-of-paint (earlier today, live):** cyan→Iris violet everywhere, aurora heroes, ALL decorative animation + cyber gridding stripped, term-labels ~1.6× bold, Pulse on the full $15,800. (See 2026-07-03 handoff.)

## ⭐ MAKE EVERY DOLLA HOLLA — the zero-based/commit build (IN PROGRESS)
**Spec v2 is canonical: `docs/make-every-dolla-holla-spec.md`.** Model (confirmed w/ Scott): give every $ of the $15,800 a job. "Reserved" auto-$2,000 GOES AWAY → replaced by **committed** moves. A $ only leaves the $15,800 when you hit **Commit** = "I physically moved cash checking→savings." Stashes renamed **"Have To's / Want To's"** (have-to=obligations: taxes/insurance/CC fees; want-to=goals: trips/remodel). Each pot: planned monthly fill + Commit toggle (per-stash, per-month) → funds pot + ticks ETA. Commit persists via the **existing `DeployConfirmation` store, `lane = stash.id`** (already generalized — no new store). Two surfaces: the big card (create+watch-grow) AND confirm-lines at the bottom of "Make Every Dolla Holla." Lumpy-bill alarm (chunk D) only fires if the pot didn't cover it. Free nudge (E) = adult-funny "give it a job."

- **Chunk A ✅** — `SinkingFund.kind` ('have_to'|'want_to') + `monthlyFill`; StashesCard renamed + grouped (amber have-to / violet want-to) + per-card kind toggle + split "+ Have-to/+ Want-to" buttons. Taxes tagged have_to, Trips want_to (data migration, backed up `scripts/sinkingFunds-backup-2026-07-04.json`).
- **Chunk B ✅** — commit lines at bottom of BudgetPulse (each pot: kind badge, planned move, Commit toggle) + "committed this month" tally. Persists (verified across reloads). **Plus fixed a DATA-LOSS bug:** `seedDefaultStashes` only checked linked categories → since Taxes/Trips have empty categories it re-seeded dup ids → save collapsed → **deleted Trips.** Fixed (now also skips by existing id); Trips restored from backup; `stashes_seeded_v1` gated. Real money data — verify stash count after any stash change.
- **Chunk C ✅ (shipped, browser-verified) — "reserved → committed" reserve-math flip.** THE load-bearing one, done. `committedReserves(confirms, month)` in `stashMath.ts` = Σ deployConfirms where `lane.startsWith('stash-')` (all stash ids are `stash-…`; excludes 'investing'). `computeSafeToSpend` got optional 5th param `reserveOverride` (default `totalReserveSetAside()` via `??`, so 0 is honored + tests stay green). Killed the auto $2,000 off the top: Money Map "Reserves"→**"Committed"** slice (starts $0, climbs on commit), BudgetPulse header "reserved"→**"committed"**, Safe-to-Spend line→**"committed to savings"**, scorecard Cash-Flow detail→**"committed"** (+ its `reserveSetAside` var at BudgetView ~515 is now `committedForOverview` = committedReserves for the viewed month; 'avg'→''→0). `savedPrior` uses the PRIOR month's OWN committed. **AppDataContext now LOADS deployConfirmations (both load paths) → Dashboard Safe-to-Spend passes committedReserves(currentMonth) and AGREES with Budget.** +4 tests (130/130), tsc clean. **Verified in real Chrome (localhost:5173, real Postgres, July): $1,000 committed · $11,470 free · $5,345 STS all reconcile across Money Map / Pulse / Safe-to-Spend (both surfaces) / Cash-Flow.** NOTE: left `VariableSurplusCard`'s `setAsideYtd = totalReserveSetAside()*months` AS-IS on purpose — that's the PLANNED-set-aside denominator for the "free to deploy" reconciliation (intent, not moved money); flipping it to committed would make that number less honest. `totalReserveSetAside()` now means "planned monthly fill sum" (the safe default), live surfaces override with committed.
- **Chunk D (REVISED w/ Scott 2026-07-04 — design locked, NOT built; start here next).** Scott rejected building a standalone "bill→pot alarm engine" — most of it already exists. The model: (1) **split lumpy bills into their own categories** — car insurance OUT of the mixed `insurance` bucket (home ~$68/mo stays monthly), later taxes → income-tax / property-tax; recategorize + merchant mapping so it sticks. (2) **A Have-To pot card is the front door** — creating the "Car Insurance" card creates/links its category (~$267/mo = $3,200/yr); shows at bottom under its category + on the **Edit Budget screen (Scott PARKED the edit-screen detail — "I'll get back to that")**. (3) **Pot auto-drains + passes silently — ALREADY WORKS:** stash balance = contributions − linked-category spend, so the ~$1,600 charge drains the pot on its own, AND linking a category auto-puts it in the **reserve lane** (excluded from monthly over/under). ⚠️ KEY: chunk C only killed the auto-$2k SET-ASIDE, NOT the reserve LANE — the lumpy-exclusion classification is fully intact (`laneOf`/`configureStashLanes` untouched). So "preserve that lane" = already done; just link the category. (4) **NEW build = the shortfall nudge** (Scott now WANTS it): pot didn't cover the bill (e.g. set aside $900, bill $1,600) → flag the $700 gap so he makes it up later. Small: color/warn the pot card when balance goes negative — NOT a whole subsystem. Next action: show Liberty Mutual charges + confirm amounts BEFORE any recategorize.

**Chunk D — CONFIRMED DATA + decisions in flight (2026-07-04, verified against live DB; Scott dismissed the confirm-modal = wants to keep talking, DO NOT execute yet):**
- The `insurance` category holds 3 different animals: **STATE FARM ~$68/mo home** (→ fold into **Utilities**, both are fixed-lane so math-identical; Scott ok'd), **LIBERTY MUTUAL car $1,654 (5/22) + $1,637 (11/21)** ~2×/yr (→ **Car Insurance Have-To pot**, lumpy), **ALLIANZ TRAVEL $55 (3/13)** = Florida-trip one-off (→ `travel_personal`). After those move out, DELETE the `insurance` bucket.
- ⚠️ Scott SAID "get rid of utilities" but that's a SLIP — he means **Insurance**. Utilities holds ~$875/mo real bills (electric/gas/water/internet/cell) — DO NOT delete it. Re-confirm before any delete.
- Misspelled custom category **`credi_card_memberships` label "Credi Card Memberships"** ($872/yr, 4 charges) — it's a lumpy annual fee like car insurance → offer as a Have-To pot (~$73/mo) OR delete. Scott called it "Credit Card Monthly" (mis-remembered the name). Other stray custom cats seen: `weed`, `bills_misc`, `atm_cash` (icon "??" broken).
- **✅ DONE (2026-07-04, committed + browser-verified): the insurance split + cards.** Durable classifier rules in `src/utils/transactionCategorize.ts` (State Farm→utilities, Liberty Mutual→car_insurance, Allianz→travel_personal, allstate/geico/progressive→car_insurance, generic 'insurance' retired). Data migration (`scripts/insurance-migration.mjs`, backup `scripts/backups/pre-insurance-migration-2026-07-04.json`): 13 insurance rows recategorized (2→car_insurance $3,291, 1→travel_personal $55, 10→utilities State Farm), "Credi Card Memberships" label fixed→"Credit Card Memberships", `car_insurance` registered as custom category, **2 Have-To cards created** (Car Insurance $275/mo tgt $3,300, Credit Card Memberships $73/mo tgt $872) BOTH with linked `categories` → auto reserve-lane + draw-down, empty insurance bucket deleted. All verified in Chrome (cards render in Goal Tracker, no console errors). Full teller resync also done this session (all 5 accts healthy through 7/3). Tolls confirmed prepaid-topup (nothing since 5/29 is normal).
- **✅ DONE (2026-07-04, committed + browser-verified): Edit-Budget items 1 & 2.** (1) **Delete/rename categories** — each bucket row in Edit Budget has a two-click ✕ delete (guarded; investing exempt since Settings-synced) + click-to-edit label; removes the budget line only, not the txns; persists via replaceCollection. (2) **Pots count toward $15,800** — new "Have-To's / Want-To's · planned moves" section in the edit table (fill editable here OR on the card, kept in lockstep via `updatePotFill` setting both monthlyFill+monthlyContribution); `plannedPotFills` folded into `unallocated`, the allocation bar (amber segment), and the footer ("Total allocated $15,335 of $15,800, Left to allocate $465 ✓ budget complete" when gap <$500). Fixes the leftover reading ~$2k high. `BudgetView.tsx` only.
- **✅ DONE (2026-07-04, committed + browser-verified): "fixed" verbiage retired.** User-facing copy only (internal `BudgetLane` type stays 'fixed' — no refactor): edit-table "Fixed / Essential"→"Essentials", Safe-to-Spend "fixed bills"→"essential bills" (verified rendering "− $9,237 essential bills"), Monthly Detail "Fixed & On Target"→"Essentials & On Target" (sub notes "amounts vary"). Scott's point: essentials aren't fixed AMOUNTS.
- **STILL PENDING (build items, NOT done):** (a) ~~delete-category UI~~ ✅ · (b) ~~pots on Edit Budget~~ ✅ · (c) ~~"fixed" verbiage~~ ✅ · remaining: — there's currently NO way to delete a category/bucket; Scott wants one. (b) **Icons look cheap** — swap the emoji row-icons for a clean monochrome LINE-ICON set (Lucide) tinted violet, OR remove them; Scott leaning "just remove," I recommend the line-set. (c) **Edit Budget = the PLAN**: pots must appear on the Edit Budget screen with their planned monthly fill and COUNT toward the $15,800 zero-based allocation (so "add until small gap = complete" works); a pot only leaves the MONTHLY budget when COMMITTED (flexible per-month amount, e.g. $100 one month, $1,200 next). Plan (Edit Budget) vs execution (commit) stay separate; don't double-count across the two screens.
- **Chunk E** — Free "give it a job" nudge, adult-funny.

## Data facts (Scott's real numbers, confirmed)
- Insurance category MIXES: State Farm home **~$68/mo** (keep monthly) + Liberty Mutual **car ~$3,200/yr** (2×~$1,600 → a have-to) + stray Allianz $55. CC memberships **~$725-870/yr** (own category, clean to move). Taxes already a stash ($10k goal, $1,000/mo); Trips a want-to ($1,000/mo). The $2,000 reserve = Taxes+Trips.
- Insurance split / CC-fee move / Liberty-Mutual recategorization = deferred (v1's reserve-lane cascade was DROPPED as too risky; v2 commit model doesn't need it for the core).

## Gotchas
- Bash cwd resets to `C:\Claude` — `cd /c/Claude/projects/signal/signal-app &&` first. Probe scripts: `scripts/*.mjs`, `pg` + DATABASE_URL, ssl rejectUnauthorized:false. BACK UP before mutating stashes/expenses.
- `seedDefaultStashes` dup-id trap (now fixed) — watch stash count after changes.
- Chrome MCP: click Budget nav by ref (coord flaky); server id churns; reload tools via ToolSearch each session.
